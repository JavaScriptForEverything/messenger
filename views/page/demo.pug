extends ../layout
block script
	script( defer type='module' src='/js/page/demo.js')
	//- style.
		input[type="search"]::-webkit-search-decoration,
		input[type="search"]::-webkit-search-cancel-button,
		input[type="search"]::-webkit-search-results-button,
		input[type="search"]::-webkit-search-results-decoration {
			-webkit-appearance:none;
		}

block body 
	div(class='h-screen')

		div(name='demo' class='w-full p-5 ')
			//- img( name='avatar' src='/images/users/default.jpg' class='w-40 h-40')

			//- img(name='crop-preview') 
			//- br
			//- button(name='crop-button' class='border border-slate-400 px-3 py-1.5') Crop
			//- button(name='crop-download' class='border border-slate-400 px-3 py-1.5') Download

		- 
			const navTabs = ['Overview', 'Settings', 'Security', 'SMTP', 'SEO']

		ul(
			name='nav-tabs-container'
			class='flex gap-2 text-slate-500'
			class='[&>.active]:text-blue-500 [&>.active]:font-medium'
			)
			each tab in navTabs
				li(
					class='cursor-pointer' 
				)= tab
	
	script.
		//- console.log('hello')
		const $ = (selector) => document.querySelector(selector)
		const profileViewTabsContainer = $('[name=nav-tabs-container]')
		const tabs = Array.from(profileViewTabsContainer.children)

		profileViewTabsContainer.addEventListener('click', (evt) => {

			tabs.forEach( (tab, index) => {
				tab.classList.toggle('active', tab.textContent === evt.target.textContent)
				//- if( tab[index] !== tabs[0][index]) tabs[0].classList.toggle('active', true)
				//- profileViewTabsContainer.classList.toggle('active', tab.href === evt.target.href)
			})

		})
